{
  "name": "cabbuddy-root",
  "version": "1.0.0",
  "description": "Root package for CabBuddy deployment",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm install"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
