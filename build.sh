#!/bin/bash
# This script directs Render to the correct directory

# Change to the server directory
cd server

# Install dependencies
npm install

# The start command will be handled by Render's configuration
